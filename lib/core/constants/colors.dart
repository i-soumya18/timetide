import 'package:flutter/material.dart';

class AppColors {
  // Primary color palette
  static const primary = Color(0xFF219EBC);
  static const secondary = Color(0xFF8ECAE6);
  static const accent = Color(0xFFFFB703);
  static const background = Color(0xFFF5F5F5);

  // Text colors
  static const textPrimary = Color(0xFF333333);
  static const textSecondary = Color(0xFF666666);
  static const textHint = Color(0xFF999999);

  // Functional colors
  static const success = Color(0xFF4CAF50);
  static const warning = Color(0xFFFF9800);
  static const error = Color(0xFFF44336);
  static const info = Color(0xFF2196F3);

  // UI Element colors
  static const cardBackground = Colors.white;
  static const divider = Color(0xFFEEEEEE);
  static const inputBackground = Color(0xFFF9F9F9);

  // Gradient colors
  static const gradientStart = Color(0xFF219EBC);
  static const gradientEnd = Color(0xFF8ECAE6);

  // Task priority colors
  static const taskHighPriority = Color(0xFFF44336);
  static const taskMediumPriority = Color(0xFFFF9800);
  static const taskLowPriority = Color(0xFF4CAF50);
}
